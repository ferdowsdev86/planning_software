new Slider({
    appendTo : targetElement,
    width    : 200
});

new Slider({
    appendTo  : targetElement,
    width     : 200,
    showValue : 'thumb'
});

new Slider({
    appendTo  : targetElement,
    width     : 200,
    showSteps : true,
    step      : 10,
    showValue : 'thumb'
});

new Slider({
    appendTo : targetElement,
    text     : 'Text + value',
    width    : 200
});

new Slider({
    appendTo  : targetElement,
    text      : 'Text',
    showValue : false,
    width     : 200
});

new Slider({
    appendTo    : targetElement,
    text        : 'Tooltip',
    showValue   : false,
    showTooltip : true,
    width       : 200
});
