# Setup instructions

To configure the required endpoint URL:s and API keys you can either:
* rename `config.template.php` to `config.php` and add the data there, or
* add them directly to the server environment variables
